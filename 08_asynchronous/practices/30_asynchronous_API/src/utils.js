

export class InputDetails {
	constructor(type = null, name = null, isDisabled = null, value = null){
		this.type 			= type
		this.name			= name
		this.isDisabled 	= isDisabled
		this.value 			= value
	}
}

